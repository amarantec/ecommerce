version: '3.9'

services:
  db:
    image: postgres:15.2-alpine3.17
    ports:
      -5432:5432
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db